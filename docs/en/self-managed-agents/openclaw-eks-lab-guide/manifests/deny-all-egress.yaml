apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: openclaw-deny-all-egress
  namespace: openclaw-agents
spec:
  podSelector:
    matchLabels:
      app: openclaw
  policyTypes:
    - Egress
